import styled from 'styled-components';

const CurrentProgress = styled.div`
  h1{
    color: pink
  }
`

const Bar = () => {
    return (
        <CurrentProgress>
          <h1>hello</h1>
        </CurrentProgress>
    );
}

export default Bar;